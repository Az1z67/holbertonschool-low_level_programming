#include "main.h"


/**
* _abs -function for the the absolute value of an integer
*
*@n: - checks int n
* Return: 1 for letters. Returns 0 for rest
*/


int _abs(int n)
{
if (n > 0)
{
return (n);
}
else
{
return (-1 * n);
}
}
