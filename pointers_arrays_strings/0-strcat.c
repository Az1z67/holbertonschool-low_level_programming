#include "main.h"

/**
 * _strcat - function for connecting strings
 * @dest: 1st str
 * @src: 2nd str
 * Return: dest.
 */

char *_memset(char *s, char b, unsigned int n)
{
	int i = 0;
	int j = 0;

	while (dest[i] != '\0')
		i++;
	while (src[j] != '\0')
	{
		dest[i] = src[j];
		i++;
		j++;
	}
	return (dest);
}
